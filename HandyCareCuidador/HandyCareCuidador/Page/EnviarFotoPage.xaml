<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:handyCareCuidador="clr-namespace:HandyCareCuidador;assembly=HandyCareCuidador"
             x:Class="HandyCareCuidador.Page.EnviarFotoPage">
    <ScrollView>
        <StackLayout Padding="15">
            <Label Text="Descreva a foto" />
            <Image Source="{Binding FotoPaciente}"/>
            <Entry Text="{Binding Foto.FotoDescricao}" Placeholder="Descreva a foto" />
            <Button Text="Enviar" Command="{Binding EnviarCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>