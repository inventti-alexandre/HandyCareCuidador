<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:rox="clr-namespace:Rox;assembly=Rox.Xamarin.Video.Portable"
             x:Class="HandyCareCuidador.Page.EnviarVideoPage">
    <rox:VideoView x:Name="VideoView" AutoPlay="{Binding AutoPlay}" LoopPlay="{Binding LoopPlay}"
                   Muted="{Binding Muted}" Volume="{Binding Volume}" ShowController="{Binding ShowController}"
                   Source="{Binding VideoPaciente}" />
    <ScrollView>
        <StackLayout Padding="15">
            <Label Text="Descreva a foto" />
            <Entry Text="{Binding Video.VidDescricao}" Placeholder="Descreva o vídeo" />
            <Button Text="Enviar" Command="{Binding EnviarCommand}" />
        </StackLayout>
    </ScrollView>
</ContentPage>